if [ -f ~/.bashrc ]; then . ~/.bashrc; fi

npml() {
  npm list $@ | grep ^[+L]
}

alias npml=npml

****

if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

npml() {
  npm list $@ | grep ^[+L]
}

alias npml=npml